# Ralph Progress Log — JTF News Truth Bug Sweep
Started: 2026-04-14
Branch: ralph/truth-digest-fix-2026-04-14
Mission: fix every Truth-affecting bug before the next App Store submission. Submission is PAUSED.
---

## Seed context (read before first iteration)

Today is **2026-04-14**. The iPad Release build shows the Digest tab stuck at **April 8** while Stories/Archive correctly show Apr 13/14. Investigation produced three compounding root causes:

1. **Production `podcast.xml` has unresolved git merge conflict markers** at lines 1110, 1204, 1205 (`<<<<<<< Updated upstream` / `=======` / `>>>>>>> Stashed changes`). Apple's `XMLParser` aborts at line 1110 col 2 with "StartTag: invalid element name." Everything after line 1110 is lost to the app. Apr 12 is also missing from the feed; Apr 13 has duplicate episode number 1 (content issue, flagged separately).

2. **Swift `PodcastXMLParser` silently returns whatever it managed to parse** before any `didFailWithError`. No error delegate, no throw, no banner. Silent partial data is not acceptable for a Truth-seeking app.

3. **`DigestView` fetches once per process** (`.task` guarded by `hasLoadedOnce`) with no scenePhase / onAppear / reconnect hook, and no cross-check against the canonical `monitor.json.last_date`. A partial first fetch is permanent until pull-to-refresh.

There are also two smaller Truth hazards in the same parser worth fixing while we are in there:

4. Items with empty `<enclosure url>` are silently dropped at PodcastService.swift:177.
5. Items with unparseable `<pubDate>` fall back to `Date()` at line 173, which — because the list sorts newest first — would float a malformed item to the top.

## Execution model (hard rules for every iteration)

- Ralph runs on the **M4 Mac**. Working directory: `/Users/larryseyer/jtfnewsapp`.
- Ralph can run bash, Swift/Xcode edits. Ralph **cannot** run Python. Ralph **cannot** run git commands targeting `/Volumes/MacLive/`.
- Production JTF News lives on the **2012 Intel Mac**, mounted at `/Volumes/MacLive/Users/larryseyer/JTFNews`. Python there runs only under `./venv/bin/python` (system Python is 2.7 and cannot parse main.py).
- Commits to production happen via **`./bu.sh "message"`** only, never raw git.
- Server-affecting stories (APP-001, APP-019..APP-023) produce **patch documents** under `/Users/larryseyer/jtfnewsapp/docs/server_patches/`. Ralph never edits files on `/Volumes/MacLive/`. Larry applies the patches on Intel Mac via `Edit` + `bu.sh`.
- Ralph never claims runtime verification for anything Python. That's Larry's, on Intel.
- No `xcodebuild`, `swift test`, or any build during the story phase — they run once in Ralph's verification pass after all stories pass.
- One story per iteration, lowest `priority` number with `passes: false` first.
- Commit message format: `APP-XXX: <story title>`.

## Memory context (already captured, do not re-derive)

- `project_pre_appstore_freeze.md` — freeze is LIFTED as of 2026-04-14; Truth-first fixes before any submission.
- `feedback_truth_accuracy.md` — every displayed date/fact must match source on every row.
- `feedback_production_scope.md` — stay on iOS side without explicit invitation. Emergency bug is an invitation to **document** a fix, not to edit production.
- `feedback_jtfnews_production_workflow.md` — bu.sh for commits; Python Intel-only via venv.
- `reference_pbxproj_ids.md` — next free pbxproj IDs at story start: `AA000001084 / AA000002071`. Stories that add files (APP-014, APP-016) must consume sequentially and update the memory.
- `project_widget_architecture.md` — shared SwiftData schema across app and widget; new models must be added to the shared container.
- `feedback_swiftdata_contexts.md` — @Query only sees writes on the same ModelContext the view injected.
- `feedback_elegance_standard.md` — prefer the clean refactor over the minimal patch.

## Story dependency map (for iteration planning)

```
APP-001  (server runbook) ──── independent
APP-002  (parser error surfacing) ─┐
APP-003  (drop filter)             ├─ parser Truth fixes; independent of each other
APP-004  (pubDate fallback)        ┘
APP-005  (monitor.json decode) ──── enables APP-010

APP-006  (cooldown key) ──── enables APP-007, APP-008, APP-009
APP-007  (scenePhase refetch) ─┐
APP-008  (onAppear refetch)    ├─ refetch gap fixes
APP-009  (reconnect refetch)   ┘
APP-010  (mismatch banner) ──── depends on APP-002 (malformedXML type) + APP-005 (fetchCanonicalLastDate)

APP-011  (currentEpisode split + pending row UI) ─── depends on APP-003 (hasAudio)
APP-012  (freshness label) ──── depends on APP-006 (digest cooldown key)
APP-013  (macOS scenePhase un-guard) ──── independent

APP-014  (test target + XML fixtures) ──── consumes hasAudio from APP-003 and malformedXML from APP-002
APP-015  (monitor.json tests) ──── depends on APP-005

APP-016  (CachedPodcastEpisode + @Query) ──── structural; keep after core Truth fixes
APP-017  (offline banner) ──── depends on APP-016

APP-018  (watchOS audit) ──── independent

APP-019  (atomic writes patch doc)      ─┐
APP-020  (Archive reachability patch)    ├─ server-side patch docs; Larry applies on Intel
APP-021  (single-commit push patch)      │
APP-022  (server tests patch)            │
APP-023  (publish invariants doc)        ┘
```

## Notes for future iterations

- When a story creates new pbxproj entries (APP-014 adds a test target, APP-016 adds a model file), after completion **update `reference_pbxproj_ids.md`** memory with the new max IDs so the next story reads correct values.
- When a story modifies memory-relevant state (e.g., APP-023 ships the publish invariants doc to production), note it in progress.txt so the memory update happens.
- Do not delete or rewrite these seed sections — append new iteration logs below the `--- Iteration Log ---` divider.

---
## --- Iteration Log ---
(Ralph appends one block per completed story below.)

### APP-001 — Emergency runbook: strip git conflict markers from production podcast.xml
**Status:** DONE
**Type:** Server patch document
**Files created:**
- `docs/server_patches/APP-001-podcast-xml-conflict-markers.md`

**What was done:**
Created a complete remediation runbook for Larry to apply on the Intel Mac. The document covers:
- Exact observation: conflict markers at lines 1110, 1204, 1205 causing XMLParser abort
- Root-cause hypothesis: `git stash pop` left markers, published via main.py API path bypassing bu.sh
- Step-by-step safe recipe: pause main.py, inspect, sed preview+apply, verify episodes, bu.sh commit, restart
- Flagged follow-up items: missing Apr 12 episode, duplicate episode number on Apr 13
- Cross-referenced APP-019 (atomic writes) and APP-021 (single-commit push) as structural prevention

**Patterns/learnings:**
- Server patch documents are the correct delivery mechanism for production fixes — Ralph documents, Larry applies.
- The conflict markers explain why Digest showed Apr 8: XMLParser aborts at the first invalid element, returning only pre-abort items.

### APP-002 — PodcastXMLParser: surface parse errors instead of silently returning a partial list
**Status:** DONE
**Type:** Code
**Files modified:**
- `JTFNews/Services/PodcastService.swift`

**What was done:**
- Added `PodcastFeedError` enum with `.malformedXML(underlying:partialCount:)` case.
- Added `parseError` property to `PodcastXMLParser`, set by new `parser(_:parseErrorOccurred:)` and `parser(_:validationErrorOccurred:)` delegate methods.
- Renamed core method to `parseThrowing()`: throws `.malformedXML` when parseError is set AND fewer than 7 items parsed. When ≥7 items parsed (late-feed abort), returns what was parsed with a DEBUG-only log.
- Kept `parse()` as a deprecated wrapper that swallows to empty with DEBUG print.
- `fetchEpisodes` now calls `parseThrowing()` and propagates errors to the view layer.

**Patterns/learnings:**
- `minTrustedCount = 7` threshold: a full week's feed is the minimum for partial data to be trustworthy. Below that, the abort happened too early for the data to be reliable.

### APP-003 — Stop silently dropping episodes with empty enclosure URL
**Status:** DONE
**Type:** Code
**Files modified:**
- `JTFNews/Services/PodcastService.swift`

**What was done:**
- Removed the `if !episode.audioURL.isEmpty` guard — all episodes are now appended regardless of audio state.
- Added `var hasAudio: Bool` computed property to `PodcastEpisode`.
- Changed ID generation: audioless episodes use `"pending-\(title)-\(isoDate)"` for a stable, collision-free Identifiable ID. Episodes with audio keep using the audio URL as ID.
- Added DEBUG-only print for audioless episodes to aid local testing.

**Patterns/learnings:**
- `ISO8601DateFormatter` is used for the date key in pending IDs; falls back to raw pubDate text if date parsing failed (though APP-004 will drop those items entirely).

### APP-004 — Drop items with unparseable pubDate instead of falling back to `Date()`
**Status:** DONE
**Type:** Code
**Files modified:**
- `JTFNews/Services/PodcastService.swift`

**What was done:**
- Replaced `date ?? Date()` with a `guard let date = parseRFC2822Date(...)` that returns early (drops the item) on failure.
- DEBUG-only log prints the unparseable date and title for diagnosis.
- `PodcastEpisode.date` remains non-optional `Date` — the drop happens before construction.
- Simplified the audioless ID path since `date` is now guaranteed non-nil past the guard.

**Patterns/learnings:**
- Guard-return in `didEndElement` is clean because XMLParser won't skip the next `<item>` — the parser resets state at `didStartElement("item")`.

### APP-005 — Decode monitor.json.daily_digest.last_date + podcast_updated; add fetchCanonicalLastDate()
**Status:** DONE
**Type:** Code
**Files modified:**
- `JTFNews/Services/PodcastService.swift`

**What was done:**
- Extended `DailyDigestInfo` with `lastDate: String?` (CodingKey `last_date`) and `podcastUpdated: Bool?` (CodingKey `podcast_updated`).
- Added `fetchCanonicalLastDate(baseURL:)` to `PodcastService` — reads `monitor.json` with `.reloadRevalidatingCacheData`, parses `last_date` as `yyyy-MM-dd` in GMT, returns nil if absent or malformed.
- No caller wiring yet — APP-010 will integrate into DigestView.

**Patterns/learnings:**
- Using `.reloadRevalidatingCacheData` consistently across force-refresh paths ensures the freshness guarantee matches `fetchEpisodes(force: true)`.

### APP-006 — Add FetchCooldownKey.digest (60 s)
**Status:** DONE
**Type:** Code
**Files modified:**
- `JTFNews/Services/FetchCooldown.swift`

**What was done:**
- Added `FetchCooldownKey.digest = "lastDigestFetch"`.
- Added `FetchCooldownInterval.digestShort = 60` (60 seconds).
- No DigestView wiring — APP-007 will consume.

### APP-007 — DigestView: foreground refetch via scenePhase
**Status:** DONE
**Type:** Code
**Files modified:**
- `JTFNews/Views/Digest/DigestView.swift`

**What was done:**
- Added `@Environment(\.scenePhase)` to DigestView.
- Added `.onChange(of: scenePhase)` on NavigationStack: on `.active`, checks connectivity and 60s cooldown before launching `loadContent(force: true)`.
- Added `FetchCooldown.markFetched(key: .digest)` in the episodes fetch success path (inside `@MainActor` block).
- Cold launch behavior unchanged — `hasLoadedOnce` path still runs first.

### APP-008 — DigestView: tab-reentry refetch via onAppear
**Status:** DONE
**Type:** Code
**Files modified:**
- `JTFNews/Views/Digest/DigestView.swift`

**What was done:**
- Added `.onAppear` to the inner `VStack(spacing: 0)` inside NavigationStack.
- Guards on `hasLoadedOnce` (skip first appearance — `.task` handles that), `connectivity.isConnected`, and 60s cooldown.
- On pass, launches `Task { await loadContent(force: true) }`.

### APP-009 — DigestView: reconnect refetch when connectivity flips false → true
**Status:** DONE
**Type:** Code
**Files modified:**
- `JTFNews/Views/Digest/DigestView.swift`

**What was done:**
- Added `.onChange(of: connectivity.isConnected)` on NavigationStack.
- Triggers only on `false → true` transition, guards on `hasLoadedOnce` and 60s cooldown.
- Launches `Task { await loadContent(force: true) }` on pass.

### APP-010 — DigestView: cross-check parsed feed against monitor.json.last_date and banner on mismatch
**Status:** DONE
**Type:** Code
**Files modified:**
- `JTFNews/Views/Digest/DigestView.swift`

**What was done:**
- Added `@State feedMismatchBanner`, `didAutoRetryMismatch`, and `canonicalLastDate`.
- Added fourth child task in `loadContent` calling `fetchCanonicalLastDate()`.
- After task group: compares canonical date vs newest episode date at GMT day granularity. If canonical is newer, auto-retries once; on second mismatch sets user-visible banner.
- `.malformedXML` errors from parseThrowing() now set banner "Feed is temporarily malformed — showing what we could parse".
- Banner renders in `pinnedHeader` below segmented picker, `.jtfCaption` `.secondary`, tap to dismiss.
- `didAutoRetryMismatch` resets at start of non-retry `loadContent` calls.

**Patterns/learnings:**
- `@State` used as task-group-to-post-group communication channel since Swift concurrency closures can't capture mutable locals. After `withTaskGroup` returns, all tasks have completed and @State is current on MainActor.

### APP-011 — DigestView: split currentEpisode; render audio-pending rows non-tappable with clock icon
**Status:** DONE
**Type:** Code
**Files modified:**
- `JTFNews/Views/Digest/DigestView.swift`

**What was done:**
- Replaced `currentEpisode` with `currentAudioEpisode` (first with audio matching selection) and `currentVideoEpisode` (always first episode).
- `audioSection` uses `currentAudioEpisode`; `currentVideoURL` uses `currentVideoEpisode`.
- `episodeRow` branches on `hasAudio`: audioful rows render as Button with speaker icon; audioless rows render as plain HStack with clock icon and "Audio pending" trailing text.
- Audioless rows match padding/clipShape for visual consistency.
- Accessibility labels: "now playing" for active audio rows, "audio pending" for audioless rows.

### APP-012 — Shared FetchCooldown.relativeLastUpdated helper + DigestView 'Updated Nm ago' label
**Status:** DONE
**Type:** Code
**Files modified:**
- `JTFNews/Services/FetchCooldown.swift`
- `JTFNews/Views/Stories/StoriesView.swift`
- `JTFNews/Views/Digest/DigestView.swift`

**What was done:**
- Added `relativeLastUpdated(for:)` and `isStale(for:threshold:)` to `FetchCooldown` with 20-minute default threshold.
- StoriesView's `lastUpdatedText` and `isStale` now delegate to shared helpers. Behavior identical.
- DigestView renders "Updated Xm ago" in `pinnedHeader` below video/audio section when stale or offline. Hidden when mismatch banner is showing to avoid clutter.

### APP-013 — Un-guard macOS scenePhase in JTFNewsApp.swift
**Status:** DONE
**Type:** Code
**Files modified:**
- `JTFNews/App/JTFNewsApp.swift`

**What was done:**
- Removed `#if os(iOS)` / `#endif` wrapping the `.onChange(of: scenePhase)` block.
- Both iOS and macOS now call `BackgroundRefreshManager.performForegroundCheck()` on `.active` transition.
- Also removed the long comment that was iOS-specific — the behavior is now platform-parity.

### APP-014 — Add JTFNewsTests target + XML parser fixtures
**Status:** DONE
**Type:** Code (test target + fixtures)
**Files created:**
- `JTFNewsTests/PodcastXMLParserTests.swift`
- `JTFNewsTests/Fixtures/fixture_fullFeed.xml`
- `JTFNewsTests/Fixtures/fixture_partialFeed.xml`
- `JTFNewsTests/Fixtures/fixture_malformedPubDate.xml`
- `JTFNewsTests/Fixtures/fixture_conflictMarkers.xml`
**Files modified:**
- `JTFNews.xcodeproj/project.pbxproj`

**What was done:**
- Created `JTFNewsTests` target (Swift Testing, `com.apple.product-type.bundle.unit-test`) wired in pbxproj.
- 4 fixture XML files: full feed (3 items), partial (no enclosure on middle), malformed pubDate, conflict markers.
- 5 tests: full feed parse, partial hasAudio check, malformed date drop, conflict marker throws, legacy parse() crash safety.
- Used pbxproj IDs AA000001084-AA000001088 / AA000002071-AA000002075 / AA000003004 / AA000005020-AA000005021.

**Patterns/learnings:**
- Updated `reference_pbxproj_ids.md` memory with new max IDs. Next free: AA000001089 / AA000002076.
- Test bundle uses `Bundle(for: BundleToken.self)` to find fixtures in the test bundle's Resources.

### APP-015 — MonitorResponse decode tests
**Status:** DONE
**Type:** Code (tests + fixtures)
**Files created:**
- `JTFNewsTests/MonitorResponseTests.swift`
- `JTFNewsTests/Fixtures/fixture_monitor_full.json`
- `JTFNewsTests/Fixtures/fixture_monitor_minimal.json`
**Files modified:**
- `JTFNews.xcodeproj/project.pbxproj`

**What was done:**
- 3 tests: full decode (all fields surface), minimal decode (optional fields nil, no crash), date parsing verification (2026-04-13 parses correctly in GMT).
- Added source/resource entries to test target's build phases.
- Used pbxproj IDs AA000001089-AA000001091 / AA000002076-AA000002078.
- Updated `reference_pbxproj_ids.md` memory. Next free: AA000001092 / AA000002079.

### APP-016 — CachedPodcastEpisode SwiftData model + DigestView @Query migration
**Status:** DONE
**Type:** Code (new model + migration)
**Files created:**
- `JTFNews/Models/CachedPodcastEpisode.swift`
**Files modified:**
- `JTFNews/App/JTFNewsApp.swift` (schema)
- `JTFNewsWidget/SharedModelContainer.swift` (schema)
- `JTFNews/Views/Digest/DigestView.swift` (full @Query migration)
- `JTFNews.xcodeproj/project.pbxproj`

**What was done:**
- Created `@Model CachedPodcastEpisode` with unique `id`, all episode fields, plus `hasAudio` and `lastSeenAt`.
- Added to both app and widget schema arrays.
- DigestView: replaced `@State episodes` with `@Query(sort: \.date, order: .reverse)` on CachedPodcastEpisode.
- Added `@Environment(\.modelContext)` for persistence.
- Added `persistEpisodes(_:)` method: upserts fetched episodes keyed on `id`, purges items with `lastSeenAt` older than 7 days.
- Updated all references: `episodes` → `cachedEpisodes`, type refs from `PodcastEpisode` → `CachedPodcastEpisode`.
- Used pbxproj IDs AA000001092-AA000001093 / AA000002079.

**Patterns/learnings:**
- Per `feedback_swiftdata_contexts.md`: persist MUST use the SwiftUI-injected modelContext so @Query sees writes.
- Widget target also compiles CachedPodcastEpisode.swift so the shared schema stays in sync.

---

### Iteration 17 — APP-017: DigestView: offline banner when rendering cached data

**Files changed:**
- `JTFNews/Views/Digest/DigestView.swift`

**What was done:**
- Added offline banner in `episodesContent`: when `!connectivity.isConnected && !cachedEpisodes.isEmpty`, renders `HStack { wifi.slash icon + "Showing cached digest (offline)" }` styled `.jtfCaption` + `.secondary` above the episode list.
- Added offline empty state: when `!connectivity.isConnected && cachedEpisodes.isEmpty && !isLoading`, renders "No internet connection — the digest will load when you're back online." instead of the generic "No episodes available" text.
- Suppressed mismatch banner when offline: `pinnedHeader` now guards `feedMismatchBanner` display with `connectivity.isConnected` so the offline banner in `episodesContent` takes precedence — avoids stacking two status banners.

**Patterns/learnings:**
- When layering multiple status banners (mismatch, offline, freshness), establish a clear precedence: offline > mismatch > freshness. One banner at a time in each visual zone.

---

### Iteration 18 — APP-018: Audit watchOS WatchDataService for silent-drop patterns

**Files scanned:**
- `JTFNewsWatch/WatchDataService.swift` — full read
- `JTFNewsWatch/WatchContentView.swift` — full read
- `JTFNewsWatch/JTFNewsWatchApp.swift` — full read (schema only, no data logic)
- `JTFNewsWatch/WatchStoryRow.swift` — full read (display only, no data logic)
- `JTFNewsWatch/JTFNewsWatchWidget.swift` — full read (widget timeline provider)

**Findings:**
1. **Truth hazard (fixed):** `WatchDataService.swift:39` — `parseDate(dto.publishedAt) ?? Date()` — same silent date fallback as APP-004. Unparseable publishedAt becomes `Date()`, sorting a bad story to the top. Fixed: guard-let with continue + print, matching APP-004 pattern.
2. **Silent error swallow (fixed):** `WatchContentView.swift:48-50` — empty catch block with comment "Silently fail". Replaced with `print()` for debuggability. The empty-state UI is honest ("No Stories Yet" + pull-to-refresh) so no UI change needed.
3. **Clean:** `sourceURLs ?? [:]` and `status ?? ""` on existing-story updates — normal optional handling, not Truth hazards (empty values are semantically correct for missing optional fields).
4. **Clean:** Widget `fetchEntry()` catch returns "Unable to load" fallback — appropriate for a widget with no interactive error recovery.

**Files changed:**
- `JTFNewsWatch/WatchDataService.swift` (guard-let instead of ?? Date())
- `JTFNewsWatch/WatchContentView.swift` (print error instead of silent swallow)

**Patterns/learnings:**
- The `?? Date()` anti-pattern for date parsing is now confirmed present in both iOS (PodcastService, fixed in APP-004) and watchOS (WatchDataService, fixed here). Any future date parsing should use guard-let + skip/log.

---

### Iteration 19 — APP-019: Server patch doc: atomic_write_text helper + every publish call site

**Files created:**
- `docs/server_patches/APP-019-atomic-writes.md`

**What was done:**
- Documented the `atomic_write_text` helper using `os.replace()` (atomic POSIX rename).
- Listed all 5 in-place write call sites in main.py (podcast.xml, stories.json, corrections.json, monitor.json, archive/index.json) with approximate line numbers.
- Included a worked before/after diff for one call site (stories.json).
- Provided application instructions (grep → edit → bu.sh → restart), verification steps (curl + JSON validation), and scope notes (Ralph cannot edit main.py; Larry applies via bu.sh).
- Cross-linked APP-021 as the complementary remote-publish atomicity layer.

---

### Iteration 20 — APP-020: Server patch doc: Archive.org reachability gate

**Files created:**
- `docs/server_patches/APP-020-archive-reachability-gate.md`

**What was done:**
- Documented the race condition: `ia.upload()` acks before Archive.org makes the file publicly retrievable.
- Proposed `wait_for_archive_reachability` with exponential backoff (2s→64s, ~2 min cap).
- Showed before/after diffs for `upload_to_archive_org` and `update_podcast_feeds`.
- Specified `monitor.json.daily_digest.upload_status` field ("success" | "pending").
- Provided application and verification instructions.

---

### Iteration 21 — APP-021: Server patch doc: single-commit GitHub Pages push

**Files created:**
- `docs/server_patches/APP-021-single-commit-ghpages.md`

**What was done:**
- Documented the per-file PUT race where Fastly sees multiple ref states mid-cycle.
- Proposed 4-step Git Data API recipe: create blobs → build tree → create commit → update ref.
- Included complete Python pseudo-code for `atomic_push_to_ghpages`.
- Explained failure handling: steps 1-3 failing leave old ref intact; step 4 race is retryable.
- Cross-linked APP-019 as the complementary local-write atomicity layer.

---

### Iteration 22 — APP-022: Server patch doc: tests for atomic_write + publish gate

**Files created:**
- `docs/server_patches/APP-022-server-tests.md`

**What was done:**
- Provided complete `test_atomic_writes.py`: concurrent reader/writer test (1 MB payloads, 200 write cycles), basic correctness test, and file-creation test.
- Provided complete `test_publish_gate.py`: 5 test cases covering immediate success, retry-then-success, timeout→pending, 206 partial content, and network error resilience.
- Both test files include inline function copies for self-contained execution before APP-019/020 are applied.
- Provided run instructions for Intel Mac via `./venv/bin/python -m pytest`.

---

### Iteration 23 — APP-023: Server doc: publish_invariants.md

**Files created:**
- `docs/server_patches/APP-023-publish-invariants.md`

**What was done:**
- Wrote the publish invariants document with three invariants: (1) atomic file replacement, (2) enclosure reachability, (3) atomic publish cycle.
- Each invariant includes: statement, implementation summary, scope, and cross-reference to its APP story.
- Included a "Before You Change Publish Code" checklist with 5 items.
- Added incident reference section explaining the April 2026 root causes.
- Document is formatted for Larry to drop at `/Volumes/MacLive/Users/larryseyer/JTFNews/docs/publish_invariants.md` via bu.sh.

---

## Summary

**All 23 stories now pass.** The sweep covered:
- APP-001: Emergency server patch document for conflict markers
- APP-002–005: Parser error surfacing, no-silent-drop, date guard, monitor cross-check
- APP-006–009: FetchCooldown key, scenePhase/onAppear/connectivity refetch hooks
- APP-010–012: Canonical date cross-check, episode split + audio-pending rows, shared freshness helpers
- APP-013: macOS scenePhase un-guard
- APP-014–015: Unit test target + XML parser/monitor fixtures
- APP-016: SwiftData CachedPodcastEpisode model
- APP-017: Offline banner for cached digest data
- APP-018: watchOS audit (fixed ?? Date() + silent catch)
- APP-019–023: Server patch documents (atomic writes, reachability gate, single-commit push, tests, invariants)
