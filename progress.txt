## Multi-Platform Support (iPad + macOS)

Design spec: docs/superpowers/specs/2026-04-07-multi-platform-design.md

### Key Facts
- 27 of 30 Swift files are already cross-platform compatible
- Only 4 files need changes: project.pbxproj, BackgroundRefreshManager.swift, YouTubePlayerView.swift, ContentView.swift, JTFNewsApp.swift
- iPad is already in device family (1,2) — just needs verification
- macOS 14.0 minimum for SwiftData support
- Use #if os(iOS) / #if os(macOS) conditionals — no separate targets

### Codebase Patterns
- Xcode project generated manually (no Xcode IDE) — project.pbxproj uses AA-prefixed IDs
- SwiftData models use default initializers with property defaults (Swift 6 strict concurrency)
- New Swift files MUST be added to project.pbxproj in 4 places: PBXBuildFile, PBXFileReference, PBXGroup children, PBXSourcesBuildPhase
- IMPORTANT: Story model uses `storyHash` (not `hash`) — `hash` shadows NSObject.hash and crashes SwiftData
- IMPORTANT: StoriesResponse.source is String? (optional) — the JSON field is missing from the API
- IMPORTANT: Always do clean Release builds (iOS caches aggressively)
- Build command pattern: xcodebuild clean -scheme JTFNews ... && xcodebuild -scheme JTFNews -configuration Release ...

### Previous Work (APP-015 through APP-019) — All Complete
See git log for details.

---

## Progress

### APP-020: Add macOS destination to Xcode project ✅
- Modified: JTFNews.xcodeproj/project.pbxproj
- Changes:
  - Project-level Debug + Release: SDKROOT changed from `iphoneos` to `auto`, added `SUPPORTED_PLATFORMS = "iphoneos iphonesimulator macosx"`, added `MACOSX_DEPLOYMENT_TARGET = 14.0`
  - Target-level Debug + Release: Added `SUPPORTS_MACCATALYST = NO` and `SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD = NO` (native macOS, not Catalyst)
  - Target-level Debug + Release: Split `LD_RUNPATH_SEARCH_PATHS` into SDK-conditioned variants (macOS uses `@executable_path/../Frameworks`)
  - iOS deployment target remains 17.0, TARGETED_DEVICE_FAMILY remains 1,2
- Note: macOS build won't succeed yet — needs APP-021 through APP-024 for platform-conditional code

### APP-021: Wrap BackgroundRefreshManager in #if os(iOS) ✅
- Modified: JTFNews/Services/BackgroundRefreshManager.swift, JTFNews/App/JTFNewsApp.swift
- BackgroundRefreshManager.swift: entire enum wrapped in `#if os(iOS)` / `#endif` (after `import Foundation`)
- JTFNewsApp.swift: `BackgroundRefreshManager.register()` in init() and `BackgroundRefreshManager.scheduleRefresh()` in onAppear both wrapped in `#if os(iOS)` guards

### APP-022: Make YouTubePlayerView cross-platform ✅
- Modified: JTFNews/Views/Digest/YouTubePlayerView.swift
- Split into `#if os(iOS)` (UIViewRepresentable) and `#elseif os(macOS)` (NSViewRepresentable) blocks
- Shared logic (HTML template, extractVideoID, loadVideoIfNeeded) extracted to extensions
- Coordinator shared, URL opening uses platform-conditional `UIApplication.shared.open` vs `NSWorkspace.shared.open`
- macOS version omits `scrollView.isScrollEnabled` (not available on macOS WKWebView)
- Pattern: use extensions for shared cross-platform logic to avoid duplication

### APP-023: Add platform-conditional navigation (sidebar vs tabs) ✅
- Modified: JTFNews/App/ContentView.swift
- iOS: unchanged TabView with Stories/Digest/Archive tabs + MiniPlayerView overlay
- macOS: NavigationSplitView with sidebar List (JTF News, Daily Digest, Archive) and detail switch
- MiniPlayerView on macOS: no 49pt tab bar offset (sits at bottom of detail area)
- Default selection is 0 (Stories/JTF News) on both platforms

### APP-024: Add macOS Settings scene and window configuration ✅
- Modified: JTFNews/App/JTFNewsApp.swift
- macOS: Added `Settings { SettingsView() }` scene for Cmd+, menu bar access
- macOS: `.defaultSize(width: 1000, height: 700)` on WindowGroup
- macOS: `.windowResizability(.contentMinSize)` + `.frame(minWidth: 800, minHeight: 600)` on ContentView
- iOS: gear icon sheet in StoriesView unchanged
- Settings scene needs `.modelContainer(sharedModelContainer)` since it's a separate scene

### APP-025: Verify iPad layout and test all platforms ✅
- Modified: JTFNews/Services/AudioManager.swift
- Found and fixed: `AVAudioSession` is iOS-only — wrapped `configureAudioSession()` body in `#if os(iOS)` guard
- Also fixed: `var info` → `let info` in `updateNowPlayingInfo()` (unused mutability warning)
- Verified: `UserNotifications` (UNUserNotificationCenter) available on macOS 10.14+, no guard needed
- Verified: `MediaPlayer` (MPRemoteCommandCenter, MPNowPlayingInfoCenter) available on macOS 10.12.2+, no guard needed
- Verified: No UIKit imports in any view files
- Verified: iOS-only Info.plist keys (UIBackgroundModes, etc.) are ignored by Xcode for macOS builds
- NOTE: Actual build verification (xcodebuild) deferred to verification phase per Ralph instructions

